<UserControl x:Class="WPF_Project_1.View.UserControl.SideBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Project_1.View.UserControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" Width="200">
    <Grid Background="Transparent">

        
        <StackPanel>
            <Button Style="{StaticResource SidePanelButtonStyle}" Content="ТЮНЕР"  Command="{Binding ShowTunerPageCommand}"/>
            <Button Style="{StaticResource SidePanelButtonStyle}" Content="АКОРДИ" Command="{Binding ShowChordsPageCommand}"/>
            <Button Style="{StaticResource SidePanelButtonStyle}" Content="НАЛАШТУВАННЯ" Command="{Binding ShowSettingsPageCommand}" FontSize="22"/>
        </StackPanel>
    </Grid>
</UserControl>
